<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title>RifqyDesign</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&family=Poppins:wght@400;600&family=Roboto:wght@400;700&family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg:#0f1115; --text:#e7e9ee; --muted:#9aa1ac;
      --card:#141821; --card2:#1a2030; --border:#252b3a;
      --primary:#5b8cff; --secondary:#22c55e; --accent:#ffb020;
    }
    *{box-sizing:border-box}
    html,body{margin:0;padding:0;background:var(--bg);color:var(--text);font-family:'Inter',system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial,sans-serif}
    a{color:inherit}
    .wrap{max-width:1100px;margin:0 auto;padding:16px}
    header{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:8px 0}
    .title{display:flex;align-items:center;gap:10px}
    .title h1{font-size:1.25rem;margin:0}
    .subtitle{color:var(--muted);font-size:0.9rem}
    .controls{display:flex;gap:8px;flex-wrap:wrap}
    .btn{border:none;border-radius:10px;padding:10px 14px;font-weight:600;cursor:pointer;transition:transform .06s ease,opacity .2s}
    .btn:active{transform:scale(0.98)}
    .btn.primary{background:var(--primary);color:#fff}
    .btn.secondary{background:#2a3347;color:#fff;border:1px solid var(--border)}
    .btn.ghost{background:transparent;color:var(--text);border:1px solid var(--border)}
    .layout{display:grid;grid-template-columns:1fr;gap:14px}
    @media(min-width:960px){ .layout{grid-template-columns:320px 1fr} }
    .card{background:var(--card);border:1px solid var(--border);border-radius:16px;padding:16px}
    .card h2{margin:0 0 12px;font-size:1.05rem}
    .row{display:grid;grid-template-columns:1fr 1fr;gap:10px}
    @media(min-width:640px){ .row.three{grid-template-columns:repeat(3,1fr)} }
    label{display:block;font-size:0.85rem;font-weight:600;margin-bottom:6px;color:var(--muted)}
    input[type="color"],select,input[type="text"],input[type="number"]{
      width:100%;padding:10px;border-radius:10px;border:1px solid var(--border);
      background:#0c1018;color:var(--text)
    }
    .swatch{display:flex;gap:8px;flex-wrap:wrap}
    .swatch .chip{flex:1;min-width:120px;border-radius:12px;padding:10px;border:1px solid var(--border);background:var(--card2)}
    .chip .hex{font-family:monospace;font-size:0.9rem}
    .chip small{color:var(--muted)}
    .preview{background:#ffffff;border-radius:16px;border:1px solid #e8eaf0;color:#1b1e25}
    .preview-inner{padding:18px}
    .preview-toolbar{display:flex;gap:8px;align-items:center;padding:12px;border-bottom:1px solid #e8eaf0;background:#f7f8fb;border-radius:16px 16px 0 0}
    .preview-toolbar .tag{font-size:0.8rem;color:#556075}
    .p-btn{padding:10px 14px;border:none;border-radius:10px;font-weight:700;cursor:pointer}
    .p-btn.primary{background:var(--primary);color:#fff}
    .p-btn.secondary{background:var(--secondary);color:#fff}
    .p-btn.accent{background:var(--accent);color:#1b1e25}
    .p-link{color:var(--primary);text-decoration:none;font-weight:600}
    .section{display:grid;grid-template-columns:1fr;gap:12px;margin-top:10px}
    .section.two{grid-template-columns:1fr 1fr}
    @media(min-width:720px){ .section.two{grid-template-columns:1fr 1fr} }
    .badge{display:inline-block;padding:6px 10px;border-radius:999px;background:#eef2ff;color:#334155;font-weight:700;font-size:0.8rem}
    .export{display:flex;gap:8px;flex-wrap:wrap;margin-top:8px}
    textarea{width:100%;height:140px;border-radius:12px;border:1px solid var(--border);background:#0c1018;color:var(--text);padding:12px;font-family:ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas,"Liberation Mono","Courier New",monospace}
    .mini{font-size:0.8rem;color:var(--muted);margin-top:8px}
    .grid{display:grid;grid-template-columns:1fr;gap:12px}
    @media(min-width:760px){ .grid{grid-template-columns:repeat(2,1fr)} }
    @media(min-width:1024px){ .grid{grid-template-columns:repeat(3,1fr)} }
    .comp{border:1px dashed #d1d7e0;border-radius:12px;padding:12px;background:#f9fafb}
    .comp h3{margin:0 0 8px;font-size:1rem}
    .copy{display:flex;gap:8px;flex-wrap:wrap}
    .toast{position:fixed;bottom:16px;left:50%;transform:translateX(-50%);background:#1a2030;color:#fff;border:1px solid var(--border);padding:10px 14px;border-radius:10px;font-weight:600;opacity:0;pointer-events:none;transition:opacity .25s}
    .toast.show{opacity:1}
    .brand{display:flex;align-items:center;gap:8px}
    .brand .logo{width:28px;height:28px;border-radius:8px;background:linear-gradient(135deg,var(--primary),var(--accent));border:1px solid var(--border)}
  </style>
</head>
<body>
  <div class="wrap">
    <header>
      <div class="title">
        <div class="brand">
          <div class="logo"></div>
          <h1>RifqyDesign</h1>
        </div>
        <span class="subtitle">Playground untuk warna, font, dan komponen — mobile-first.</span>
      </div>
      <div class="controls">
        <button id="toggleTheme" class="btn ghost">🌗 Mode</button>
        <button id="resetBtn" class="btn secondary">↺ Reset</button>
        <button id="exportCSSBtn" class="btn primary">⬇️ Export CSS</button>
        <button id="exportJSONBtn" class="btn secondary">⬇️ Export JSON</button>
      </div>
    </header>

    <div class="layout">
      <!-- Panel pengaturan -->
      <section class="card">
        <h2>Pengaturan</h2>
        <div class="row three">
          <div>
            <label for="colorPrimary">Warna utama</label>
            <input id="colorPrimary" type="color" value="#5b8cff">
          </div>
          <div>
            <label for="colorSecondary">Warna sekunder</label>
            <input id="colorSecondary" type="color" value="#22c55e">
          </div>
          <div>
            <label for="colorAccent">Warna aksen</label>
            <input id="colorAccent" type="color" value="#ffb020">
          </div>
        </div>

        <div class="row">
          <div>
            <label for="fontSelect">Font</label>
            <select id="fontSelect">
              <option value="Inter" selected>Inter</option>
              <option value="Poppins">Poppins</option>
              <option value="Roboto">Roboto</option>
              <option value="Montserrat">Montserrat</option>
            </select>
          </div>
          <div>
            <label for="radiusInput">Radius komponen (px)</label>
            <input id="radiusInput" type="number" min="0" max="24" value="10">
          </div>
        </div>

        <div class="row">
          <div>
            <label for="sampleHeading">Judul preview</label>
            <input id="sampleHeading" type="text" value="Judul Preview">
          </div>
          <div>
            <label for="sampleText">Teks preview</label>
            <input id="sampleText" type="text" value="Halo, ini RifqyDesign!">
          </div>
        </div>

        <div class="swatch" style="margin-top:12px">
          <div class="chip">
            <div><strong>Primary</strong></div>
            <div class="hex" id="hexPrimary">#5B8CFF</div>
            <small>Untuk aksi utama</small>
          </div>
          <div class="chip">
            <div><strong>Secondary</strong></div>
            <div class="hex" id="hexSecondary">#22C55E</div>
            <small>Aksi pendukung</small>
          </div>
          <div class="chip">
            <div><strong>Accent</strong></div>
            <div class="hex" id="hexAccent">#FFB020</div>
            <small>Penekanan/brand</small>
          </div>
        </div>

        <div class="export">
          <button id="copyPaletteBtn" class="btn ghost">📋 Copy palet HEX</button>
        </div>
        <textarea id="exportOutput" readonly placeholder="Hasil export CSS/JSON akan muncul di sini..."></textarea>
        <div class="mini">Tip: kamu bisa simpan JSON untuk dipakai ulang sebagai tema.</div>
      </section>

      <!-- Panel preview -->
      <section class="card preview">
        <div class="preview-toolbar">
          <span class="tag">Preview komponen</span>
          <span class="badge">Mobile-first</span>
        </div>
        <div class="preview-inner" id="previewInner">
          <h1 id="previewHeading" style="margin:0 0 10px">Judul Preview</h1>
          <p id="previewText">Halo, ini RifqyDesign!</p>

          <div class="section two">
            <div class="comp">
              <h3>Tombol</h3>
              <div style="display:flex;gap:8px;flex-wrap:wrap">
                <button class="p-btn primary">Primary</button>
                <button class="p-btn secondary">Secondary</button>
                <button class="p-btn accent">Accent</button>
                <button class="p-btn" style="background:#e5e7eb;color:#111">Neutral</button>
              </div>
            </div>

            <div class="comp">
              <h3>Kartu</h3>
              <div style="display:grid;grid-template-columns:1fr;gap:8px">
                <div style="border:1px solid #e4e7ef;border-radius:var(--radius);padding:12px;background:#fff">
                  <div style="font-weight:700;color:#111;margin-bottom:6px">Kartu judul</div>
                  <div style="color:#4b5565">Deskripsi singkat komponen.</div>
                  <div style="margin-top:10px;display:flex;gap:8px">
                    <a class="p-link" href="#">Tautan</a>
                    <button class="p-btn primary">Aksi</button>
                  </div>
                </div>
              </div>
            </div>

            <div class="comp">
              <h3>Form</h3>
              <div class="grid">
                <div>
                  <label style="color:#475569">Email</label>
                  <input type="text" placeholder="nama@contoh.com" style="width:100%;padding:10px;border:1px solid #d5dae5;border-radius:var(--radius)"/>
                </div>
                <div>
                  <label style="color:#475569">Password</label>
                  <input type="text" placeholder="••••••••" style="width:100%;padding:10px;border:1px solid #d5dae5;border-radius:var(--radius)"/>
                </div>
                <div style="grid-column:1/-1;display:flex;gap:8px;align-items:center">
                  <button class="p-btn secondary">Masuk</button>
                  <a class="p-link" href="#">Lupa password?</a>
                </div>
              </div>
            </div>

            <div class="comp">
              <h3>Tag & Badges</h3>
              <div style="display:flex;gap:8px;flex-wrap:wrap">
                <span class="badge" style="background:#eef2ff;color:#334155">Info</span>
                <span class="badge" style="background:#dcfce7;color:#14532d">Sukses</span>
                <span class="badge" style="background:#fff7ed;color:#7c2d12">Peringatan</span>
              </div>
            </div>
          </div>
        </div>
      </section>
    </div>
  </div>

  <div class="toast" id="toast">Disalin ke clipboard</div>

  <script>
    // Elements
    const colorPrimary = document.getElementById('colorPrimary');
    const colorSecondary = document.getElementById('colorSecondary');
    const colorAccent = document.getElementById('colorAccent');
    const fontSelect = document.getElementById('fontSelect');
    const radiusInput = document.getElementById('radiusInput');
    const sampleHeading = document.getElementById('sampleHeading');
    const sampleText = document.getElementById('sampleText');
    const previewHeading = document.getElementById('previewHeading');
    const previewText = document.getElementById('previewText');
    const exportOutput = document.getElementById('exportOutput');
    const toggleTheme = document.getElementById('toggleTheme');
    const resetBtn = document.getElementById('resetBtn');
    const exportCSSBtn = document.getElementById('exportCSSBtn');
    const exportJSONBtn = document.getElementById('exportJSONBtn');
    const copyPaletteBtn = document.getElementById('copyPaletteBtn');
    const hexPrimary = document.getElementById('hexPrimary');
    const hexSecondary = document.getElementById('hexSecondary');
    const hexAccent = document.getElementById('hexAccent');
    const toast = document.getElementById('toast');

    // Theme state
    const state = {
      theme: localStorage.getItem('rifqyDesignTheme') || 'dark'
    };

    function applyTheme(mode){
      if(mode === 'light'){
        document.documentElement.style.setProperty('--bg','#f5f7fb');
        document.documentElement.style.setProperty('--text','#10131a');
        document.documentElement.style.setProperty('--muted','#5e6a7f');
        document.documentElement.style.setProperty('--card','#ffffff');
        document.documentElement.style.setProperty('--card2','#f7f9ff');
        document.documentElement.style.setProperty('--border','#e3e7f0');
      } else {
        document.documentElement.style.setProperty('--bg','#0f1115');
        document.documentElement.style.setProperty('--text','#e7e9ee');
        document.documentElement.style.setProperty('--muted','#9aa1ac');
        document.documentElement.style.setProperty('--card','#141821');
        document.documentElement.style.setProperty('--card2','#1a2030');
        document.documentElement.style.setProperty('--border','#252b3a');
      }
      localStorage.setItem('rifqyDesignTheme', mode);
    }

    // Update design tokens
    function updateDesign(){
      document.documentElement.style.setProperty('--primary', colorPrimary.value);
      document.documentElement.style.setProperty('--secondary', colorSecondary.value);
      document.documentElement.style.setProperty('--accent', colorAccent.value);
      document.documentElement.style.setProperty('--radius', `${radiusInput.value}px`);
      document.body.style.fontFamily = `${fontSelect.value}, system-ui, sans-serif`;
      previewHeading.textContent = sampleHeading.value;
      previewText.textContent = sampleText.value;
      hexPrimary.textContent = colorPrimary.value.toUpperCase();
      hexSecondary.textContent = colorSecondary.value.toUpperCase();
      hexAccent.textContent = colorAccent.value.toUpperCase();

      // Apply radius to preview components via CSS var
      document.querySelectorAll('.p-btn').forEach(el => el.style.borderRadius = `${radiusInput.value}px`);
      document.querySelectorAll('.badge').forEach(el => el.style.borderRadius = `${Math.max(12, Number(radiusInput.value))}px`);
    }

    // Export handlers
    function exportCSS(){
      const css = `
:root {
  --primary: ${colorPrimary.value};
  --secondary: ${colorSecondary.value};
  --accent: ${colorAccent.value};
  --radius: ${radiusInput.value}px;
}
body {
  font-family: ${fontSelect.value}, system-ui, sans-serif;
}
.button-primary {
  background: var(--primary);
  color: #fff;
  border-radius: var(--radius);
  padding: 10px 14px;
  border: none;
}
.card {
  border: 1px solid #e4e7ef;
  border-radius: var(--radius);
  background: #fff;
}
`.trim();
      exportOutput.value = css;
      copyToClipboard(css);
    }

    function exportJSON(){
      const json = {
        tokens: {
          primary: colorPrimary.value,
          secondary: colorSecondary.value,
          accent: colorAccent.value,
          radius: `${radiusInput.value}px`,
          font: fontSelect.value
        },
        components: {
          button: { padding: '10px 14px', radius: `${radiusInput.value}px` },
          card: { border: '#e4e7ef', radius: `${radiusInput.value}px`, bg: '#ffffff' }
        }
      };
      const txt = JSON.stringify(json, null, 2);
      exportOutput.value = txt;
      copyToClipboard(txt);
    }

    // Clipboard + toast
    async function copyToClipboard(text){
      try {
        await navigator.clipboard.writeText(text);
        showToast('Disalin ke clipboard');
      } catch(e) {
        showToast('Gagal menyalin');
      }
    }
    function showToast(msg){
      toast.textContent = msg;
      toast.classList.add('show');
      setTimeout(()=>toast.classList.remove('show'), 1400);
    }

    // Reset to defaults
    function reset(){
      colorPrimary.value = '#5b8cff';
      colorSecondary.value = '#22c55e';
      colorAccent.value = '#ffb020';
      fontSelect.value = 'Inter';
      radiusInput.value = 10;
      sampleHeading.value = 'Judul Preview';
      sampleText.value = 'Halo, ini RifqyDesign!';
      updateDesign();
      exportOutput.value = '';
      showToast('Di-reset');
    }

    // Event bindings
    [colorPrimary, colorSecondary, colorAccent, fontSelect, radiusInput, sampleHeading, sampleText]
      .forEach(el => el.addEventListener('input', updateDesign));

    toggleTheme.addEventListener('click', () => {
      state.theme = state.theme === 'dark' ? 'light' : 'dark';
      applyTheme(state.theme);
    });

    exportCSSBtn.addEventListener('click', exportCSS);
    exportJSONBtn.addEventListener('click', exportJSON);
    copyPaletteBtn.addEventListener('click', () => {
      const text = `Primary: ${colorPrimary.value}\nSecondary: ${colorSecondary.value}\nAccent: ${colorAccent.value}`;
      exportOutput.value = text;
      copyToClipboard(text);
    });

    resetBtn.addEventListener('click', reset);

    // Init
    applyTheme(state.theme);
    updateDesign();
  </script>
</body>
</html>
